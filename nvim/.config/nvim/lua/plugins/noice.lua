return {
  "folke/noice.nvim",
  opts = {
    routes = {
      -- show recording message
      -- {
      --   view = "notify",
      --   filter = { event = "msg_showmode" },
      -- },
    },
  },
}
